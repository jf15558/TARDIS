% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_geoglist.R
\name{extract_geoglist}
\alias{extract_geoglist}
\title{extract_geoglist}
\usage{
extract_geoglist(geog, geom, layer = NULL)
}
\arguments{
\item{geog}{\code{geoglist}. The output of \code{rast_to_geoglist()}.}

\item{geom}{\verb{sf simple features collection}. A set of sf geometries which
will be used to extract values from geog.}

\item{layer}{\code{numeric}. If not NULL, then an integer specifying from which
layer in \code{geog} values are to be extracted. This argument is intended for
use with user-designed \code{geom} objects which do not already contain layer
assigments, unlike returns from other TARDIS functions.}
}
\value{
\verb{sf simple features collection}. A simple features collection of points
corresponding to the centroids of all cells in geog intersected by an input
geometry (denoted by \verb{$feature}) in its specified layer (\verb{$layer}), and the
value present at that point (\verb{$value}).
}
\description{
Extract values from the layers in a geoglist object, using an sf geometry
collection. This geometry is expected to be from another TARDIS function
(e.g, \code{least_cost()}, \code{isochrone()}, ect), but could also be a user-designed
geometry.
}
\examples{
\dontrun{
library(terra)
library(TARDIS)

gal <- galapagos()
gal_m <- classify(gal, matrix(c(-Inf, 0, NA, 0, Inf, 1), ncol = 3, byrow = T), right = F)

rasts <- rast_to_geoglist(gal, gal_m, as.hex = T, hex = 6)
rlink <- link_islands(rasts)
rtd <- build_tardis(rasts, times = c(seq(2.25, 0, -0.5), 0))
org <- rbind(c(-89.78873, -1.420627, 2),
             c(-89.58525, -1.473917, 2))
dst <- rbind(c(-88.70836, -0.2627832, 2),
             c(-90.44276,  0.2943382, 2))

rpts <- point_check(rtd, rbind(org, dst))
rlcp <- least_cost(rtd, origin = rpts[1,], dest = rpts[3,])
vals <- extract_geoglist(rasts, rlcp)
}
}
