% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cost_surface.R
\name{cost_surface}
\alias{cost_surface}
\title{cost_surface}
\usage{
cost_surface(tardis, weights = "gdist", verbose = T)
}
\arguments{
\item{tardis}{An object of class 'tardis'}

\item{weights}{A character string denoting the weighting scheme to visualise.
By default these are true geographic distances (gdist). Alternatively, the
name of a weighting scheme added to the tardis object with weight_tardis().}

\item{verbose}{A logical indicating whether function progress should be
reported to the user.}
}
\value{
A geoglist with layers corresponding to the original landscape
layers in the graph, and cell values calculated as the mean of its inbound edge weights.
}
\description{
Convenience function to visualise the weighting scheme for the layers in a
tardis object.
}
\examples{
\dontrun{
#library(terra)
#library(TARDIS)

# load data
gal <- galapagos()
gal_m <- classify(gal, matrix(c(-Inf, 0, NA, 0, Inf, 1), ncol = 3, byrow = T), right = F)

# make geoglist and add links
rasts <- rast_to_geoglist(gal, gal_m)
rlink <- link_islands(rasts)

rtd <- build_tardis(rasts, times = c(seq(2.25, 0, -0.5), 0))
cost_surface(rtd)
}
}
